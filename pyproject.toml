[project]
name = "continuityos"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "google-adk>=1.0.0",
  "google-genai>=1.0.0",
  "httpx>=0.27.0",
]

[tool.vercel]
entrypoint = "api.investigate:handler"
